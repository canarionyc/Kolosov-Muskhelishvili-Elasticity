# Jupytext configuration file

# Documentation: https://jupytext.readthedocs.io/en/latest/config.html

[jupytext]
# Pair notebooks with percent scripts
formats = "ipynb,py:percent"

# Filter metadata to avoid noisy diffs
notebook_metadata_filter = "-all"
cell_metadata_filter = "-all"
